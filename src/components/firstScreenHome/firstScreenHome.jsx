import React from 'react';
import { Header } from '../header/header';
import styles from './firstScreenHome.module.scss'

export const FirstScreenHome = (props) => (
    <div className={styles.firstScreen}>
        <Header/>
        
    </div>
)